-- Bixor Trading Engine Database Schema
-- Simple initialization - Extensions only
-- All schema changes should be done via migrations

-- Enable required PostgreSQL extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Note: All tables, indexes, and data should be created via migrations
-- Run migrations in database/migrations/ folder in order 